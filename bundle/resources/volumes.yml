resources:
  volumes:
    inbound_data_volume:
      name: ${var.UC_VOLUME_PREFIX}inbound_data_volume
      schema_name: ${var.UC_SCHEMA_PREFIX}turbine
      catalog_name: etl_project
      comment: Volume for inbound data